# 参数配置模块需求文档

> 本模块是**工业视觉检测软件**的子模块之一，负责整个软件的参数管理与配置 UI。
> 上层软件包含：图像采集、图像处理与算法、AI 推理、结果输出、通信、日志、用户权限等多个并行模块。
> 本文档仅描述"参数配置模块"自身的职责、数据规范与对外接口；不涉及其他模块的内部实现。

---

## 1. 模块定位

### 1.1 在主软件中的角色

参数配置模块是工业视觉检测软件的**横向支撑模块**——它不直接参与图像处理或 AI 推理流程，但为所有算法模块、AI 模块、硬件控制模块提供运行所需的参数。其角色相当于整个软件的"参数中枢"：

```
                   ┌──────────────────────────────┐
                   │     参数配置模块（本模块）     │
                   │  JSON 解析 / UI / 校验 / 权限 │
                   └──────────────────────────────┘
                       ▲                    │
              读/写参数 │                    │ 提供参数
                       │                    ▼
   ┌──────────┬──────────────┬──────────────┬──────────┐
   │ 图像采集  │ 图像处理算法  │   AI 推理     │ 通信/输出 │
   └──────────┴──────────────┴──────────────┴──────────┘
                  （主软件其他业务模块）
```

### 1.2 模块的职责边界

**本模块负责**：

- 加载、解析、校验、保存 JSON 参数配置文件
- 为工程师提供参数编辑 UI（自动渲染）
- 按用户权限等级控制参数的可见与可编辑性
- 向其他业务模块提供参数读取接口
- 多配置文件管理（不同设备/产线/工程的切换）

**本模块不负责**：

- 具体算法的实现（属于图像处理模块）
- AI 模型的训练与推理（属于 AI 模块）
- 用户登录与角色管理（依赖用户权限模块的鉴权结果）
- 硬件 I/O（属于硬件控制模块）

### 1.3 现状与必要性

公司现有多套视觉检测软件（硅片/方棒缺陷检测、导片机检测等），均使用 JSON 文件作为参数配置载体。从已有的四份样本（`111111111111.json`、`新建文本文档.json`、`意大利.json`、`导片机.json`）可以看出，这些配置文件遵循同一套底层规范，但在演化过程中出现了结构不统一、字段类型混用、跨模块字段重复定义等问题。

为统一规范、降低维护成本、支持后续扩展（多产品线、多语言、多权限场景），需要将参数配置抽取为独立模块，制定**通用参数配置规范**，并实现配套的解析、校验、UI 渲染与权限控制能力，供主软件其他模块复用。

---

## 2. 模块目标

1. 制定统一的 JSON 参数配置数据规范，主软件下所有产品线复用同一套结构。
2. 模块提供完整能力链：自动解析配置 → 自动渲染 UI → 自动校验输入 → 按权限控制可见与可编辑性 → 保存回写 → 向其他模块提供读取接口。
3. 支持快速扩展：新增检测项、新增参数、新增数据类型时，只需修改 JSON，不修改本模块代码、也不修改其他业务模块代码。
4. 支持多语言（中/英）与多设备/多工程配置文件并存。
5. 解耦——本模块对其他业务模块零侵入，业务模块通过统一接口读参数，不直接读 JSON。

---

## 3. 数据结构规范

### 3.1 层级结构

配置文件采用**树形结构**，从顶层向下递归：

```
配置文件
├── 顶层模块（如 AI、检测参数）
│   ├── 子模块（如 初始化设置、隐裂、破片分割）
│   │   ├── 参数项（末端节点，含 Competence/Type/Limit/Value 四字段）
│   │   └── 参数项
│   └── 参数项（允许子模块层直接挂参数项，如"半片方向选择"）
└── 顶层模块
```

**关键规则**：层级深度不固定，分组节点下既可以挂下一级分组，也可以直接挂末端参数项。解析器通过判断"节点是否包含 `Type` 字段"来区分分组与末端参数：

- 含 `Type` → 末端参数
- 不含 `Type` → 分组，继续递归

### 3.2 末端参数的统一字段

每个末端参数必须包含以下四个字段，缺一不可：

| 字段 | 含义 | 类型 |
|---|---|---|
| `Competence` | 操作权限等级 | int |
| `Type` | 数据类型 | string（枚举） |
| `Limit` | 取值约束 | 取决于 Type |
| `Value` | 当前生效值 | 取决于 Type |

### 3.3 支持的数据类型（Type）

| Type 值 | 含义 | Limit 格式 | Value 示例 |
|---|---|---|---|
| `int` | 整数 | `[min, max]` 闭区间 | `60` |
| `float` | 浮点数 | `[min, max]` 闭区间 | `3.5` |
| `bool` | 布尔 | `""`（空） | `"TRUE"` 或 `"FALSE"`（大写字符串） |
| `string` | 任意字符串 | `""`（无约束） | `"Breakage|Cracks"` |
| `string_enum` | 字符串枚举 | `["选项1","选项2",...]` | `"GPU"` |
| `int_enum` | 整数枚举 | `[1, 2, 4, 8]` | `4` |
| `float_enum` | 浮点数枚举 | `[0.5, 1.0, 1.5, 2.0]` | `1.5` |
| `int[]` | 整数数组（变长） | `[min, max]`，作用于**每个元素** | `[10, 20, 30]` |
| `float[]` | 浮点数数组（变长） | `[min, max]`，作用于**每个元素** | `[0.5, 1.5, 2.5]` |
| `string[]` | 字符串数组（变长） | `""`（无约束） | `["A","B","C"]` |
| `directory` | 目录路径 | `""` | `"./json"` |
| `path` | 文件路径 | `""` | `".\\mask.hobj"` |

后续如需扩展（如 `color`、`point`、`region`），按相同模式增加即可。

### 3.4 权限等级（Competence）

| 等级 | 角色 | 权限 |
|---|---|---|
| `0` | 操作员 | 仅可见，不可编辑 |
| `1` | 产线工程师 | 可编辑日常调优参数 |
| `2` | 系统工程师 | 可编辑算法相关参数 |
| `3` | 管理员/开发 | 可编辑所有参数（含底层阈值） |

数字越大权限越高。UI 应根据当前登录用户的等级，自动隐藏或锁定高于其等级的参数项。

---

## 4. 数据规范化要求（修复历史问题）

为统一新旧文件，所有配置文件需遵循以下硬性规则：

1. **`Competence` 字段必须为 int**，禁止使用字符串 `"3"`。
2. **`bool` 类型的 `Value` 必须为字符串 `"TRUE"` / `"FALSE"`**（统一使用大写），禁止使用 JSON 原生布尔值 `true` / `false`，也禁止小写形式 `"true"` / `"false"`。解析器读取时按字符串比较判断真假。
3. **同类参数禁止双语顶层并列**。如发现 `检测参数` 与 `Detection parameters` 同时存在，需合并为单一顶层，多语言通过外部翻译表实现，不污染数据结构。
4. **同一缺陷类别的所有参数必须聚合在该类别节点下**。例如 `MASK路径` 与 `面积筛选` 应同属 `隐裂` 节点（参考 `导片机.json` 的合并方式）。

---

## 5. 功能需求

### 5.1 配置文件读写

- 加载指定路径的 JSON 配置文件，递归解析为内存树。
- 保存内存树回写到 JSON 文件，保留原始字段顺序。
- 支持加载失败时给出明确错误（语法错误、缺字段、Type 未知等）。

### 5.2 参数 UI 自动渲染

UI 框架根据节点 `Type` 字段自动选择控件：

| Type | UI 控件 |
|---|---|
| `int` / `float` | 数字输入框 + 滑块（带 min/max 提示） |
| `bool` | 开关 / 复选框 |
| `string` | 单行文本框 |
| `string_enum` / `int_enum` / `float_enum` | 下拉选择框（仅可选 Limit 列表中的值） |
| `int[]` / `float[]` / `string[]` | 多输入框组合，每个元素一个输入框；右侧带 `＋` / `−` 按钮，点击 `＋` 追加一项、`−` 删除最后一项（或每行独立删除按钮）。新增项的默认值：数字类型用 0 或 Limit 下限，字符串用空串 |
| `directory` | 目录选择对话框 |
| `path` | 文件选择对话框 |

分组节点渲染为可折叠面板或 Tab，层级深度不限。

### 5.3 输入校验

- `int` / `float`：值必须落在 `Limit` 区间内。
- `string_enum` / `int_enum` / `float_enum`：值必须为 `Limit` 列表中的某一项（区分类型严格匹配，不允许自由输入区间外的值）。
- `int[]` / `float[]`：数组的**每一个元素**都必须落在 `Limit` 区间内；空数组（长度 0）默认允许，如需限制最少元素数请在跨字段校验规则中单独声明。
- `string[]`：元素无内容约束；如需限制元素数量或单个元素格式（如非空、长度上限），通过外置校验规则配置。
- `directory` / `path`：保存时检查路径是否存在，不存在则警告（非阻断）。
- **跨字段交叉校验**：例如同一缺陷下，"面积筛选"应小于等于"允许最大面积"。校验规则可外置为独立配置文件，不写死在代码里。

### 5.4 权限控制

- 模块启动时**从用户权限模块**获取当前登录用户的 `Competence` 等级（不自行管理用户登录）。
- 渲染时：参数 `Competence` ≤ 当前用户等级 → 可编辑；> 当前等级 → 只读或隐藏（策略可配置）。
- 保存时：再次校验权限，禁止越权写入。

### 5.5 多配置文件管理

- 同时支持多份配置文件并存（按设备/产线/工程命名，如 `导片机.json`、`意大利.json`）。
- 提供切换、复制、对比功能。
- 工程切换时按 `工程0检测类别` 类似的字段确定 AI 模型类别顺序，并通知主软件下游模块（AI 模块、算法模块）刷新参数。

### 5.6 多语言支持

- 参数键名（如 `隐裂`、`Breakage`）通过独立的多语言映射表显示，不影响 JSON 内部存储。
- 显示语言可在运行时切换。

### 5.7 对外接口（向主软件其他模块）

本模块对其他业务模块提供以下接口（具体语言形式为类成员函数 / 信号槽 / 服务调用，由主软件框架决定）：

| 接口 | 方向 | 说明 |
|---|---|---|
| `GetValue(path)` | 业务模块 → 本模块 | 按点路径读取参数值，例：`GetValue("检测参数/隐裂/允许最大面积")` |
| `SetValue(path, value)` | 业务模块 → 本模块 | 编程方式写入参数（通常仅供主软件内部用，UI 编辑走另一路） |
| `Reload(filePath)` | 业务模块 → 本模块 | 重新加载配置文件（如换工程后） |
| `OnParamChanged(path)` | 本模块 → 业务模块 | 参数被用户修改后，向订阅方推送变更事件，业务模块据此热更新内部状态 |
| `OnConfigSwitched(file)` | 本模块 → 业务模块 | 配置文件被切换时通知所有订阅方 |

业务模块**禁止直接读写 JSON 文件**，必须通过上述接口访问，以保证权限控制、变更通知、缓存一致性。

---

## 6. 参数分类清单（基于现有文件归纳）

下列分类为当前 JSON 配置文件中已涉及的参数集合，本模块需要支持以下场景的渲染与管理。**注意：这里说的"模块"是 JSON 内部的逻辑分组（顶层键），不是主软件意义上的代码模块。**

### 6.1 AI 参数组

- 初始化设置：工程目录、检测核心（CPU/GPU）、检测类别清单
- 硬件设置：计算设备
- 软件设置：分类尺寸、AI 调试开关

### 6.2 图像预处理参数组

- 阈值类参数（最小阈值、最大阈值）

### 6.3 分割算法参数组（按缺陷类型分组）

- 破片分割、裂纹分割、倒角分割、硅落分割
- 每个分割算法的参数因算法而异，但统一遵循四字段规范
- 大多包含 `开启AI分类` 开关，串联传统算法与 AI 二次分类

### 6.4 缺陷判定参数组（按缺陷类型分组）

每种缺陷统一包含五项判定参数：

- 面积筛选（最小检出面积）
- 允许最大面积
- 允许总面积
- 允许个数
- 允许最大长度

涉及缺陷类型：隐裂、崩边、缺口、缺角、脏污、划伤、叠片、穿孔、线痕、薄片、其他，以及英文版的 Breakage / Cracks / Scratches / Dirty / DarkSpots / BlackSpots 等。

### 6.5 缺陷区域配置

每个缺陷类别可独立配置 MASK 路径（Halcon `.hobj` 文件），定义检测的有效区域。

### 6.6 全局参数

如导片机的"半片方向选择"（横/竖），属于检测参数顶层下的散参数。

---

## 7. 扩展性要求

1. **新增缺陷类别**：仅需在 JSON 中增加节点，复制现有缺陷的五项判定参数模板即可，UI 自动适配，无需修码。
2. **新增数据类型**：在解析器中注册新的 `Type` 枚举值与对应控件，所有现有参数不受影响。
3. **新增模块**：在顶层增加新键，按层级嵌套写入参数即可。
4. **配置版本管理**：建议在文件中增加 `Version` 顶层字段（如 `"Version": "1.0.0"`），便于后续做兼容升级。

---

## 8. 非功能需求

- **性能**：单份配置文件参数量级 ≤ 1000 项时，加载与渲染应在 500ms 内完成。
- **健壮性**：单个参数解析失败不应导致整个文件加载失败，应记录错误并继续。
- **可追溯性**：每次保存应记录修改人、修改时间、修改前后的 diff，便于产线问题回溯。
- **兼容性**：解析器需向下兼容现有四份历史文件（即使不规范，也能加载并提示需要升级）。

---

## 9. 待确认事项

以下事项需要相关方进一步确认后补入正式版：

1. `Competence` 等级的具体角色定义（上文 0-3 为推测，需与实际权限体系对齐）。
2. 是否需要参数级的"修改提示"（如修改某参数后弹出影响说明）。
3. 是否需要"出厂默认值"与"用户值"双轨制，支持一键恢复出厂。
4. 多语言映射表的存放方式（独立文件 / 内置 / 在线翻译服务）。
5. 跨字段校验规则的配置格式与作用域。

---

*文档版本：v0.1（初稿）*
*基于样本：111111111111.json / 新建文本文档.json / 意大利.json / 导片机.json*
